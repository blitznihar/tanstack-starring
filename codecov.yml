coverage:
  precision: 2
  round: down
  range: "80...100"
  status:
    project:
      default:
        target: 80%
        threshold: 2%
        if_ci_failed: error
    patch:
      default:
        target: 80%
        threshold: 10%
        informational: true

comment:
  layout: "reach,diff,flags,tree"
  behavior: default
  require_changes: false

ignore:
  - "src/routeTree.gen.ts"
  - "src/vite-env.d.ts"
  - "dist/**"
  - ".output/**"
  - "coverage/**"
  - "content/**"
  - "electron/**"
  - "scripts/**"
